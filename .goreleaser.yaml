archives:
- format: binary
builds:
- goos:
    - linux
    - darwin
    - windows
  goarch:
    - arm64
    - amd64

dockers:
  - image_templates:
    - "us-docker.pkg.dev/umeedefi/stack/fonzie:{{ .Arch }}-{{ .Env.GITHUB_SHA }}"
    - "us-docker.pkg.dev/umeedefi/stack/fonzie:{{ .Arch }}-{{ .Tag }}"
    goos: linux
    goarch:
    - arm64
    - amd64
